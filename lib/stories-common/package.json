{
  "name": "@stories/stories-common",
  "version": "0.1.0",
  "description": "Common utils, types and functions for Stories",
  "author": "AKKOMTECH",
  "license": "MIT",
  "private": true,
  "main": "dist/stories-common.js",
  "umd:main": "dist/stories-common.umd.js",
  "module": "dist/stories-common.mjs",
  "types": "dist/index.d.ts",
  "exports": "./dist/stories-common.modern.js",
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "prebuild2": "yarn run clean",
    "build3": "tsc -p tsconfig.json",
    "build": "microbundle -o dist/ --sourcemap false --compress false",
    "dev": "microbundle watch -o dist/ --sourcemap false --compress false",
    "test": "jest --coverage"
  },
  "devDependencies": {
    "@componentdriven/csf": "0.0.2-alpha.0",
    "@types/node": "^12.0.0",
    "microbundle": "^0.13.0",
    "rimraf": "^2.6.1",
    "typescript": "^4.1.2"
  }
}
